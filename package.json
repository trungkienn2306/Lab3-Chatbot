{
  "name": "travel-chatbot",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=web",
    "build:frontend": "npm run build --workspace=web",
    "lint:frontend": "npm run lint --workspace=web"
  }
}
